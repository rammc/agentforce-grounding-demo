# Data Model Object (DMO) – AeroLift_Document_Chunk__dlm
#
# Deklarative Spezifikation des DMOs, das auf das DLO
# AeroLift_Document_Chunk__dll mappt. 1:1-Mapping (Identity-Mapping); keine
# Harmonisation/Identity-Resolution erforderlich, weil die Source bereits
# atomar und deduplliziert ist.
#
# Hinweis: DMO-Anlage erfolgt – analog zum DLO – nicht über klassisches
# SFDX-Source-Format, sondern über Data Cloud UI / Datakit / Connect-REST-API.
# Die Demo betreibt das DMO als reinen Data-Cloud-Layer ohne CRM-Sync.

apiName: AeroLift_Document_Chunk__dlm
label: AeroLift Document Chunk
category: Other
description: |
  Konsolidiertes DMO über atomar zerlegte Dokument-Chunks. Quelle für den
  Vector/Hybrid Search Index.

primaryKey: recordId

mappings:
  - sourceDlo: AeroLift_Document_Chunk__dll
    fieldMappings:
      - source: recordId          ; target: recordId
      - source: sourceFile ; target: sourceFile
      - source: sourceSection ; target: sourceSection
      - source: chunkType  ; target: chunkType
      - source: content    ; target: content
      - source: productIds ; target: productIds
      - source: docType    ; target: docType
      - source: atexZone          ; target: atexZone
      - source: tempMaxC          ; target: tempMaxC
      - source: foerderleistungM3h; target: foerderleistungM3h
      - source: werkstoffGehaeuse ; target: werkstoffGehaeuse
      - source: ex_certified ; target: ex_certified
      - source: wirkungsgradPct ; target: wirkungsgradPct
      - source: druckMaxBar ; target: druckMaxBar
      - source: antriebsleistungKW; target: antriebsleistungKW

# Felddefinitionen sind eine 1:1-Kopie der DLO-Felder (siehe dlo/AeroLift_Document_Chunk.dlo.yaml).
# Wird hier nicht dupliziert, um Drift zu vermeiden – das DMO erbt das Schema
# vollständig vom DLO durch das Identity-Mapping.
